syntax = "proto3";
option java_outer_classname = "StudentPOJO"; // 生成的外部类名，同时也是文件名
message Student { // 会在StudentPOJO 外部类中生成一个内部类Student， 他是真正发送的POJO对象
   int32 id = 1;     // Student 类中的一个id属性， 1代表属性序号，不是值
   string name = 2;
}